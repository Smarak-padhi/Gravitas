# Normalize line endings to LF in the repository;
# git will check out with CRLF on Windows if core.autocrlf is true.
* text=auto eol=lf

# Binaries — do not touch
*.png  binary
*.jpg  binary
*.ico  binary
